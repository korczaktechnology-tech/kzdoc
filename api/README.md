# API — Korczak Documents

A API executável está implementada em `backend/src/korczak_documents/`. Esta pasta é a raiz documental da camada API prevista pela arquitetura.

A organização interna da API é feita por configurações, rotas, validações, serviços, repositórios, modelos, erros e logging.
