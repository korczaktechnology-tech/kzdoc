import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = { appId: "tech.korczak.documents", appName: "Korczak Documents", webDir: "../frontend/dist", bundledWebRuntime: false };
export default config;
